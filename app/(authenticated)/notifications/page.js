export default async function NotificationsPage() {
    const time = new Date().toISOString(); // obtiene la fecha y hora actual en formato ISO 8601
    return <p>{time} aqui solo estoy practicando server vs client, luego lo acomodo bien</p>
}